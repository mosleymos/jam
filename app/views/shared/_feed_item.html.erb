<li id="<%= feed_item.id %>">
  <%= link_to feed_item.user do%>
  	<%=image_tag feed_item.user.avatar.url(:thumb) %>
  <%end%>
  <%= link_to feed_item.user.name, feed_item.user %>
  <%= feed_item.user.microposts.first.content %>
  <br/>publié il y a <%= time_ago_in_words(feed_item.created_at) %>
</li>

